@* @model List<ChildDataViewModel>

<h2>Child's Past Data</h2>

<table class="table">
    <thead>
        <tr>
            <th>Date</th>
            <th>Description</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Date</td>
                <td>@item.Description</td>
                <td>@item.Details</td>
            </tr>
        }
    </tbody>
</table>

 *@


 <h3>
     Child past data/report will be in this page
 </h3>